# 数据库连接配置
# 复制此文件为 .env 并填入真实密码
# cp .env.example .env

DB_USER=admin
DB_PASSWORD=your_password_here
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=taobao_data

# Spark 配置（可选，有默认值）
# SPARK_MASTER=local[*]
# SPARK_DRIVER_MEMORY=4g
